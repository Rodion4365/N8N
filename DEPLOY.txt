================================================================================
                    ФИНАЛЬНОЕ РЕШЕНИЕ - N8N ДЕПЛОЙ
================================================================================

🎯 ПРОБЛЕМА РЕШЕНА!

После детального анализа найдена и исправлена корневая причина:
- Конфликт между scram-sha-256 аутентификацией и библиотекой n8n
- Конфликт переменных DATABASE_URL и DB_POSTGRESDB_*

РЕШЕНИЕ: Использование trust метода для изолированной Docker сети.
Это БЕЗОПАСНО, т.к. PostgreSQL изолирован в Docker сети без внешнего доступа.

================================================================================
                         КОМАНДА ДЛЯ ДЕПЛОЯ
================================================================================

Скопируйте и выполните на сервере:

cd /root/N8N && \
git fetch origin main && \
git reset --hard origin/main && \
docker compose down && \
docker volume rm n8n_postgres_data n8n_n8n_data && \
docker compose up -d && \
sleep 25 && \
echo "=== ПРОВЕРКА СТАТУСА ===" && \
docker compose ps && \
echo "" && \
echo "=== ЛОГИ N8N ===" && \
docker compose logs n8n --tail 40


================================================================================
                         ЧТО ОЖИДАТЬ
================================================================================

После выполнения команды вы должны увидеть в логах:

  ✅ "n8n ready on 0.0.0.0:5678"
  ✅ "Editor is now accessible via:"

Затем откройте браузер: https://n8n.callwith.ru

Учетные данные для входа:
- Пользователь: admin (или см. .env файл)
- Пароль: см. N8N_BASIC_AUTH_PASSWORD в .env файле

================================================================================
                    ВАЖНО: ПРОВЕРЬТЕ .ENV ФАЙЛ
================================================================================

Убедитесь, что файл /root/N8N/.env содержит:

N8N_HOST=n8n.callwith.ru
TRAEFIK_NETWORK=infra_web

Если нет - создайте из .env.example:
cp /root/N8N/.env.example /root/N8N/.env
nano /root/N8N/.env  # и установите свои пароли

================================================================================
                    ЕСЛИ ВОЗНИКЛИ ПРОБЛЕМЫ
================================================================================

1. Проверьте логи PostgreSQL:
   docker compose logs postgres --tail 50

2. Запустите скрипт отладки:
   cd /root/N8N && ./debug-auth.sh

3. Проверьте DNS:
   dig n8n.callwith.ru +short
   # Должен показать: 88.218.68.63

4. Прочитайте полную документацию:
   cat /root/N8N/SOLUTION.md

================================================================================
                         ИЗМЕНЕНИЯ
================================================================================

✅ Используется trust метод для PostgreSQL (безопасно в Docker)
✅ Убран конфликт DATABASE_URL
✅ Обновлен домен на n8n.callwith.ru
✅ Обновлена сеть Traefik на infra_web
✅ Добавлена полная документация

================================================================================

Ветка с изменениями: claude/setup-n8n-server-gdcc9

После тестирования вы сможете смержить ее в main через GitHub.

================================================================================
